# samp-omegq
Tool query &amp; garbage flood untuk server SA-MP (single machine). Fokus ke overload CPU dengan opcode 'c'/'d' + variasi payload. Cocok buat testing/lab pribadi di server tanpa proteksi game (EvoShield/Path.net/OVH Game dll hampir ga kena efek). Include tuning kernel, adaptive mode, raw socket, IPv6, dan stats realtime PPS/Mbps
